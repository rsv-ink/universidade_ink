<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <title>Admin · Universidade</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <script src="https://cdn.tailwindcss.com"></script>
  <%= stylesheet_link_tag "universidade/application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <style>
    /* Force light mode for all inputs and textareas */
    input[type="text"],
    input[type="email"],
    input[type="number"],
    input[type="password"],
    input[type="search"],
    input[type="tel"],
    input[type="url"],
    input[type="date"],
    input[type="time"],
    input[type="datetime-local"],
    textarea,
    select {
      background-color: white !important;
      color: #111827 !important;
      border-color: #d1d5db !important;
    }
    input::placeholder, textarea::placeholder { color: #9ca3af !important; }
    input:focus, textarea:focus, select:focus {
      background-color: white !important;
      border-color: #ec4899 !important;
      outline: none !important;
      box-shadow: 0 0 0 3px rgba(236, 72, 153, 0.1) !important;
    }
    input[type="checkbox"], input[type="radio"] {
      background-color: white !important;
      border-color: #d1d5db !important;
    }
    label { color: #374151 !important; }

    /* Sidebar active item */
    [data-sidebar-active="true"] {
      background-color: rgb(253 242 248);
      border-left: 3px solid rgb(236 72 153);
      padding-left: calc(0.75rem - 3px);
    }

    /* Accordion transition */
    [data-accordion-target="icon"] {
      transition: transform 0.2s ease;
    }

  </style>
</head>
<body class="bg-gray-50 antialiased overflow-hidden" style="height: 100dvh; display: flex; flex-direction: column;">

  <%# ── Modal overlay (empty = hidden) ──────────────────────── %>
  <%= turbo_frame_tag "modal" %>

  <%# ── Flash (above sidebar+panel) ────────────────────────── %>
  <% flash.each do |type, message| %>
    <div id="flash_<%= type %>"
         class="fixed top-4 right-4 z-40 px-4 py-3 rounded-lg text-sm font-medium shadow-lg
                <%= type.to_s == 'notice' ? 'bg-green-50 text-green-800 border border-green-200' : 'bg-red-50 text-red-800 border border-red-200' %>"
         data-controller="auto-dismiss">
      <%= message %>
    </div>
  <% end %>

  <%# ── Two-column shell ─────────────────────────────────────── %>
  <% hide_sidebar = controller_name == 'artigos' && (action_name == 'new' || action_name == 'edit') %>
  
  <div class="flex flex-1 min-h-0">

    <%# ── Sidebar (280px) - oculto na criação/edição de artigos ───────────────────────────────────── %>
    <% unless hide_sidebar %>
      <aside class="flex-shrink-0 bg-white border-r border-gray-200 flex flex-col" style="width: 280px;">

        <%# Header %>
        <div class="px-4 py-3 border-b border-gray-200 flex items-center justify-between flex-shrink-0">
          <span class="font-semibold text-sm text-gray-900">Universidade <span class="text-pink-500">Admin</span></span>
          <%= link_to root_path,
                class: "text-xs text-gray-400 hover:text-pink-600 transition-colors flex items-center gap-1" do %>
            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
            </svg>
            Site
          <% end %>
        </div>

        <%# Course list %>
        <div class="flex-1 overflow-y-auto" id="sidebar_cursos">
          <%= render "universidade/admin/cursos/sidebar_list",
                cursos: @sidebar_cursos, current_curso: @current_curso %>
        </div>

        <%# New course button %>
        <div class="p-3 border-t border-gray-200 flex-shrink-0">
          <%= link_to new_admin_curso_path,
                data: { turbo_frame: "modal" },
                class: "flex items-center justify-center gap-2 w-full px-3 py-2 text-sm font-medium text-pink-600 border border-dashed border-pink-300 rounded-lg hover:bg-pink-50 transition-colors" do %>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
            </svg>
            Novo curso
          <% end %>
        </div>

      </aside>
    <% end %>

    <%# ── Main panel ─────────────────────────────────────────── %>
    <main class="flex-1 overflow-y-auto bg-gray-50 min-w-0">
      <%= yield %>
    </main>

  </div><!-- end two-column -->

</body>
</html>
