<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <title>Admin · Universidade</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <script src="https://cdn.tailwindcss.com"></script>
  <%= stylesheet_link_tag "universidade/application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <style>
    /* Force light mode for all inputs and textareas */
    input[type="text"],
    input[type="email"],
    input[type="number"],
    input[type="password"],
    input[type="search"],
    input[type="tel"],
    input[type="url"],
    input[type="date"],
    input[type="time"],
    input[type="datetime-local"],
    textarea,
    select {
      background-color: white !important;
      color: #111827 !important;
      border-color: #d1d5db !important;
    }
    input::placeholder, textarea::placeholder { color: #9ca3af !important; }
    input:focus, textarea:focus, select:focus {
      background-color: white !important;
      border-color: #ec4899 !important;
      outline: none !important;
      box-shadow: 0 0 0 3px rgba(236, 72, 153, 0.1) !important;
    }
    input[type="checkbox"], input[type="radio"] {
      background-color: white !important;
      border-color: #d1d5db !important;
    }
    label { color: #374151 !important; }

    /* Accordion transition */
    [data-accordion-target="icon"] {
      transition: transform 0.2s ease;
    }
  </style>
</head>
<body class="bg-gray-50 antialiased overflow-hidden" style="height: 100dvh; display: flex; flex-direction: column;">

  <%# ── Modal overlay (persistent, shown/hidden via Stimulus) ── %>
  <div id="admin-modal" data-controller="modal" hidden>
    <div class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/50 backdrop-blur-sm"
         data-action="click->modal#closeBackdrop">
      <div class="relative bg-white rounded-2xl shadow-2xl w-full max-w-lg max-h-[90dvh] overflow-y-auto">
        <div id="modal" data-modal-target="content"></div>
      </div>
    </div>
  </div>

  <%# ── Flash ────────────────────────────────────────────────── %>
  <% flash.each do |type, message| %>
    <div id="flash_<%= type %>"
         class="fixed top-4 right-4 z-40 px-4 py-3 rounded-lg text-sm font-medium shadow-lg
                <%= type.to_s == 'notice' ? 'bg-green-50 text-green-800 border border-green-200' : 'bg-red-50 text-red-800 border border-red-200' %>"
         data-controller="auto-dismiss">
      <%= message %>
    </div>
  <% end %>

  <%# ── Top header bar ─────────────────────────────────────── %>
  <header class="flex-shrink-0 bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
    <span class="font-semibold text-sm text-gray-900">
      Universidade <span class="text-pink-500">Admin</span>
    </span>
    <%= link_to root_path,
          class: "text-xs text-gray-400 hover:text-pink-600 transition-colors flex items-center gap-1" do %>
      <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
      </svg>
      Site
    <% end %>
  </header>

  <%# ── Main content ──────────────────────────────────────── %>
  <main class="flex-1 overflow-y-auto bg-gray-50 min-w-0">
    <%= yield %>
  </main>

</body>
</html>
