<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <title>Admin · Universidade</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <script src="https://cdn.tailwindcss.com"></script>
  <%= stylesheet_link_tag "universidade/application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <style>
    /* Force light mode for all inputs and textareas */
    input[type="text"],
    input[type="email"],
    input[type="number"],
    input[type="password"],
    input[type="search"],
    input[type="tel"],
    input[type="url"],
    input[type="date"],
    input[type="time"],
    input[type="datetime-local"],
    textarea,
    select {
      background-color: white !important;
      color: #111827 !important;
      border-color: #d1d5db !important;
    }
    input::placeholder, textarea::placeholder { color: #9ca3af !important; }
    input:focus, textarea:focus, select:focus {
      background-color: white !important;
      border-color: #ec4899 !important;
      outline: none !important;
      box-shadow: 0 0 0 3px rgba(236, 72, 153, 0.1) !important;
    }
    input[type="checkbox"], input[type="radio"] {
      background-color: white !important;
      border-color: #d1d5db !important;
    }
    label { color: #374151 !important; }

    /* Accordion transition */
    [data-accordion-target="icon"] {
      transition: transform 0.2s ease;
    }

    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.5);
      z-index: 1000;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .modal-overlay.hidden {
      display: none;
    }

    .modal-container {
      background: #fff;
      width: 90%;
      max-width: 680px;
      max-height: 90vh;
      overflow-y: auto;
      border-radius: 16px;
      padding: 32px;
      position: relative;
    }

    .modal-close {
      position: absolute;
      top: 16px;
      right: 16px;
      background: none;
      border: none;
      font-size: 18px;
      cursor: pointer;
      color: #666;
    }
  </style>
</head>
<body class="bg-gray-50 antialiased overflow-hidden" style="height: 100dvh; display: flex; flex-direction: column;">

  <%# ── Modal overlay (persistent, shown/hidden via Stimulus) ── %>
  <div id="modal"
       class="modal-overlay hidden"
       data-controller="modal"
       data-action="click->modal#closeOnOverlay keydown.esc@window->modal#close">
    <div class="modal-container" data-modal-target="container">
      <button type="button" class="modal-close" data-action="click->modal#close" aria-label="Fechar modal">x</button>
      <div id="modal-content" data-modal-target="content"></div>
    </div>
  </div>

  <%# ── Flash ────────────────────────────────────────────────── %>
  <% flash.each do |type, message| %>
    <div id="flash_<%= type %>"
         class="fixed top-4 right-4 z-40 px-4 py-3 rounded-lg text-sm font-medium shadow-lg
                <%= type.to_s == 'notice' ? 'bg-green-50 text-green-800 border border-green-200' : 'bg-red-50 text-red-800 border border-red-200' %>"
         data-controller="auto-dismiss">
      <%= message %>
    </div>
  <% end %>

  <%# ── Top header bar ─────────────────────────────────────── %>
  <header class="flex-shrink-0 bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
    <div class="flex items-center gap-6">
      <span class="font-semibold text-sm text-gray-900">
        Universidade <span class="text-pink-500">Admin</span>
      </span>

      <%# Navegação entre áreas %>
      <% active_lp = controller_path == "universidade/admin/secoes" %>
      <nav class="flex items-center gap-1">
        <%= link_to admin_root_path,
              class: "px-3 py-1.5 text-sm font-medium rounded-lg transition-colors #{active_lp ? 'text-gray-500 hover:text-gray-800 hover:bg-gray-100' : 'bg-pink-50 text-pink-600'}" do %>
          Conteúdo
        <% end %>
        <%= link_to admin_secoes_path,
              class: "px-3 py-1.5 text-sm font-medium rounded-lg transition-colors #{active_lp ? 'bg-pink-50 text-pink-600' : 'text-gray-500 hover:text-gray-800 hover:bg-gray-100'}" do %>
          Landing Page
        <% end %>
      </nav>
    </div>

    <%= link_to root_path,
          class: "text-xs text-gray-400 hover:text-pink-600 transition-colors flex items-center gap-1" do %>
      <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
      </svg>
      Site
    <% end %>
  </header>

  <%# ── Main content ──────────────────────────────────────── %>
  <main class="flex-1 overflow-y-auto bg-gray-50 min-w-0">
    <%= yield %>
  </main>

</body>
</html>
