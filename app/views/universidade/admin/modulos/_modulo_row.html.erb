<%# Linha de módulo na tabela hierárquica unificada %>
<tr id="modulo_<%= modulo.id %>"
    data-sortable-id="<%= modulo.id %>"
    data-accordion-target="content"
    class="border-b border-gray-100 transition-opacity bg-white hover:bg-gray-50 <%= 'opacity-60 hover:opacity-80' unless modulo.visivel? %>">

  <%# Nome com recuo nível 1 %>
  <td class="px-4 py-2.5">
    <div class="flex items-center gap-2 pl-10">
      <div data-sortable-handle class="cursor-move text-gray-400 hover:text-gray-600 flex-shrink-0">
        <svg class="w-3.5 h-3.5" fill="currentColor" viewBox="0 0 20 20">
          <path d="M7 2a2 2 0 1 0 .001 4.001A2 2 0 0 0 7 2zm0 6a2 2 0 1 0 .001 4.001A2 2 0 0 0 7 8zm0 6a2 2 0 1 0 .001 4.001A2 2 0 0 0 7 14zm6-8a2 2 0 1 0-.001-4.001A2 2 0 0 0 13 6zm0 2a2 2 0 1 0 .001 4.001A2 2 0 0 0 13 8zm0 6a2 2 0 1 0 .001 4.001A2 2 0 0 0 13 14z"/>
        </svg>
      </div>
      <svg class="w-3.5 h-3.5 text-blue-400 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
      </svg>
      <span class="font-medium text-gray-800 text-sm <%= 'line-through text-gray-400' unless modulo.visivel? %>">
        <%= modulo.nome %>
      </span>
    </div>
    <% if modulo.descricao.present? %>
      <p class="text-xs text-gray-400 mt-0.5 pl-16 truncate max-w-lg"><%= modulo.descricao %></p>
    <% end %>
  </td>

  <%# Status %>
  <td class="px-4 py-2.5 text-center">
    <% if modulo.rascunho? %>
      <span class="inline-flex items-center gap-1.5 text-xs font-medium px-2 py-0.5 rounded-full bg-amber-100 text-amber-700">
        Rascunho
      </span>
    <% else %>
      <%= form_with url: toggle_visivel_admin_modulo_path(modulo), method: :patch, class: "inline-flex" do %>
        <% if modulo.publicado? %>
          <button type="submit" title="Clique para despublicar"
                  class="inline-flex items-center gap-1.5 text-xs font-medium px-2 py-0.5 rounded-full transition-colors bg-green-100 text-green-700 hover:bg-green-200">
            Publicado
          </button>
        <% else %>
          <button type="submit" title="Clique para publicar"
                  class="inline-flex items-center gap-1.5 text-xs font-medium px-2 py-0.5 rounded-full transition-colors bg-gray-100 text-gray-700 hover:bg-gray-200">
            Despublicado
          </button>
        <% end %>
      <% end %>
    <% end %>
  </td>

  <%# Ações %>
  <td class="px-4 py-2.5">
    <div class="flex items-center justify-end gap-1">
      <%# Adicionar trilha %>
      <%= link_to new_admin_trilha_path(modulo_id: modulo.id),
            data: { controller: "modal-link", action: "click->modal-link#open", turbo: "false" },
            title: "Adicionar trilha",
            class: "p-1.5 text-gray-400 hover:text-pink-600 hover:bg-pink-50 rounded transition-colors flex items-center gap-1 text-xs font-medium" do %>
        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
        </svg>
        Trilha
      <% end %>

      <%# Editar %>
      <%= link_to edit_admin_modulo_path(modulo),
            data: { controller: "modal-link", action: "click->modal-link#open", turbo: "false" },
            title: "Editar",
            class: "p-1.5 text-gray-400 hover:text-blue-600 hover:bg-blue-50 rounded transition-colors" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
        </svg>
      <% end %>

      <%# Excluir %>
      <%= link_to admin_modulo_path(modulo),
            data: { turbo_method: :delete, turbo_confirm: "Excluir o módulo \"#{modulo.nome}\"? Esta ação não pode ser desfeita." },
            title: "Excluir",
            class: "p-1.5 text-gray-400 hover:text-red-600 hover:bg-red-50 rounded transition-colors" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
        </svg>
      <% end %>
    </div>
  </td>
</tr>
